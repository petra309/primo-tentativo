// in interi divisione, flout sono reali in pyhon
errori sintassi, runtime o eccezione e semantica
in un programmma 5 istruzioni: input, calcolo, controllo condizioni, ripetere, generare output
= assegnare == chiedo e uguale
2 comandi, statement e espressioni
memoria libera se in un indirizzo non ho più postit
is per vedere puntare a stessa area memoria

q per uscire da help()
gggg.split
\n divide linee vuol dire no line

ricorda context manager with    e commenti molto

exit()
git status
git add .
git status
git commit -m "quel che vuoi"
git push origin main